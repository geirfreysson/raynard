node_modules
dist
target
.env
# Hand-made copies of .env still hold real provider keys.
.env.*
!.env.example
.DS_Store

# Design source archives; large binaries that do not belong in history.
*.zip

# Host-owned plugin cache written when the runner tests exercise a fixture.
scripts/fixtures/.plugin-data
extensions/.plugin-data

# Embedded Node runtime staged into the bundle by scripts/standalone-runtime.mjs.
src-tauri/binaries/
src-tauri/runtime/
src-tauri/.standalone-runtime-cache/
scripts/standalone-runtime/node_modules/
